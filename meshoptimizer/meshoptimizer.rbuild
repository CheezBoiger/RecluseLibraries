<?xml version="1.0" encoding="utf-8"?>
<project>
	<build>
		<type>Release</type>
		<install>
			<prefix path="${RECLUSE_INSTALL_PREFIX}" />
		</install>
	</build>
	<build>
		<type>Debug</type>
		<target name="meshoptimizerd" />
		<install>
			<prefix path="${RECLUSE_INSTALL_PREFIX}" />
		</install>
	</build>
</project>